<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Empleado</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" th:href="@{/css/forms.css}">
</head>
<body>

<div class="form-container">

    <span class="form-eyebrow">Gestión de Personal</span>
    <h2>Registro de Empleado</h2>
    <div class="form-header-line"></div>

    <form th:action="@{/empleados/guardar}"
          th:object="${empleado}" method="post">

        <!-- CRÍTICO: sin esto editar hace INSERT en vez de UPDATE -->
        <input type="hidden" th:field="*{codigoEmp}" />

        <div class="form-grid">

            <div class="form-group">
                <label>Nombre</label>
                <input type="text" th:field="*{nombre}" placeholder="Ej. Juan" required>
            </div>

            <div class="form-group">
                <label>Apellido</label>
                <input type="text" th:field="*{apellido}" placeholder="Ej. Pérez" required>
            </div>

            <div class="form-group">
                <label>Identificación</label>
                <input type="text" th:field="*{identificacion}" placeholder="Cédula o pasaporte" required>
            </div>

            <div class="form-group">
                <label>Email</label>
                <input type="email" th:field="*{email}" placeholder="correo@ejemplo.com">
            </div>

            <div class="form-group">
                <label>Teléfono</label>
                <input type="text" th:field="*{telefono1}" placeholder="0999 999 999">
            </div>

            <div class="form-group">
                <label>Discapacidad</label>
                <select th:field="*{discapacidad}">
                    <option value="">-- Seleccionar --</option>
                    <option value="SI">Sí</option>
                    <option value="NO">No</option>
                </select>
            </div>

            <div class="form-group">
                <label>Estado</label>
                <select th:field="*{estado}">
                    <option value="">-- Seleccionar --</option>
                    <option value="ACTIVO">Activo</option>
                    <option value="INACTIVO">Inactivo</option>
                </select>
            </div>

            <div class="form-group">
                <label>Nacionalidad</label>
                <input type="text" th:field="*{nacionalidad}" placeholder="Ej. Ecuatoriana">
            </div>

        </div>

        <div class="form-actions">
            <button type="submit" class="btn-primary"><span>Guardar</span></button>
            <a th:href="@{/empleados}" class="btn-secondary">Cancelar</a>
        </div>

    </form>

</div>

</body>
</html>